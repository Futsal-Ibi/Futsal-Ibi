/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Futsal-Ibi",
    //output: "export",
    reactStrictMode: true,
    //images: {unoptimized: true}
};

export default nextConfig;
